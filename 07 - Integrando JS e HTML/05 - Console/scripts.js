// 04/11/2022

// Limpar o console

console.clear();

// para impimir os valores ou tipos de uma variável no console

const nome = "Allan"
console.log(nome);

console.log(Allan)  // undefined

// Serve para impimir uma lista(array) em forma de tabela no console, junto dos índices

const fruits = ["Apples", "Oranges", "Bananas"]
console.table(fruits);